<p id="notice"><%= notice %></p>

<p>
  <strong>Meeting:</strong>
  <%= @token.meeting %>
</p>

<p>
  <strong>Token:</strong>
  <%= @token.token %>
</p>

<%= link_to 'Edit', edit_token_path(@token) %> |
<%= link_to 'Back', tokens_path %>
